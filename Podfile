# Uncomment this line to define a global platform for your project

target 'ScrollInScrollPages' do
  platform :ios, '8.0'
  # Uncomment this line if you're using Swift or would like to use dynamic frameworks
  #use_frameworks!

  # Pods for BeLikedClientApp
  pod 'KeepLayout/ObjC', :git => 'https://github.com/iMartinKiss/KeepLayout', :branch => 'master'

  target 'ScrollInScrollPagesTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
